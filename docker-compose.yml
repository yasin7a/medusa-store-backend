services:
  postgres:
    image: postgres:15-alpine
    container_name: medusa_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: medusa-store
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - medusa_network

  redis:
    image: redis:7-alpine
    container_name: medusa_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - medusa_network

  medusa_server:
    image: softvalley/softvalley-hub:medusa-store-backend
    container_name: medusa_server
    restart: unless-stopped
    depends_on:
      - postgres
      - redis
    environment:
      NODE_ENV: production
      MEDUSA_WORKER_MODE: server
      DISABLE_MEDUSA_ADMIN: "false"
    ports:
      - "9000:9000"
    networks:
      - medusa_network
    volumes:
      - /var/www/macrovalleyprojects/uploads:/app/.medusa/server/static      

  medusa_worker:
    image: softvalley/softvalley-hub:medusa-store-backend
    container_name: medusa_worker
    restart: unless-stopped
    depends_on:
      - postgres
      - redis
    environment:
      NODE_ENV: production
      MEDUSA_WORKER_MODE: worker
      DISABLE_MEDUSA_ADMIN: "true"
    networks:
      - medusa_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: medusa_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@macrovalleyprojects.xyz   # change this
      PGADMIN_DEFAULT_PASSWORD: admin123                     # change this
    ports:
      - "5050:80"   # pgAdmin will be accessible at http://localhost:5050
    depends_on:
      - postgres
    networks:
      - medusa_network

volumes:
  postgres_data:
    external: true

networks:
  medusa_network:
    driver: bridge
